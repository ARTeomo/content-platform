/**
 * Content schema.
 *
 * Canonical content identity and immutable versioned representations.
 * The remaining content-intelligence tables (entities, categories,
 * fingerprints, URLs) arrive in Phase 8.
 */
export * from './content-items.js';

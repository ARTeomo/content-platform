/**
 * Package barrel for @content-platform/publishers.
 *
 * Publisher and interaction adapters for external platforms. Each
 * adapter is isolated behind a narrow, testable interface so the domain
 * layer never imports provider-specific code directly.
 */

export * from './meta/index.js';
